{
  "name": "hooks-toolkit-demo",
  "version": "1.0.0",
  "description": "A demo repository for the hooks-toolkit.",
  "scripts": {
    "sandbox": "g++ -lcurl ./playground/sandbox.c -o ./playground/build/sandbox && ./playground/build/sandbox out",
    "build:hooks": "c2wasm-cli contracts build",
    "mutualfund": "yarn run build:hooks && ts-node src/mutualfund.ts",
    "daoproposal": "yarn run build:hooks && ts-node src/daoproposal.ts"
  },
  "author": {
    "name": "Denis Angell",
    "url": "https://github.com/dangell7"
  },
  "license": "MIT",
  "dependencies": {
    "@transia/hooks-toolkit": "^1.0.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
